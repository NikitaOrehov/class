#include <iostream>
#include "fraction.h"

int main(){
    Fraction fract1(-1, 2);
    Fraction fract2(-1, 2);
    // if (fract1 != fract2) std::cout<<"true";
    // else std::cout<<"false";
    // Fraction res = fract1 ^ 2;
    // std::cout<<res<<std::endl;
    ++fract1;
    //дз > < <= >= != ^ ~ ++ --
    return 0;
}